# PixelDesk 环境变量示例
# 请将此文件复制为 .env 并根据实际情况修改

# 1. 数据库配置
# 格式: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
# 在 Docker Compose 内部，HOST 通常是 db
DATABASE_URL=postgresql://pixel_user:YOUR_PG_PASSWORD_HERE@db:5432/pixeldesk
PG_PASSWORD=YOUR_PG_PASSWORD_HERE

# 2. Redis 配置
# 格式: redis://:PASSWORD@HOST:PORT
# 在 Docker Compose 内部，HOST 通常是 redis
REDIS_URL=redis://:YOUR_REDIS_PASSWORD_HERE@redis:6379
REDIS_PASSWORD=YOUR_REDIS_PASSWORD_HERE

# 3. 安全配置
# 请生成强随机字符串，例如使用 openssl rand -base64 32
JWT_SECRET=YOUR_RANDOM_JWT_SECRET
NEXTAUTH_SECRET=YOUR_RANDOM_NEXTAUTH_SECRET

# 4. 外部链接
# 部署后访问的完整 URL
NEXTAUTH_URL=https://pixel.infyniclick.com
